version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    image: api
    container_name: api
    environment:
      - config=config
    ports:
      - "3000:3000"